<pre><code class="php">
    $tab = array('a'=>'one', 'b'=>'two', 'c'=>'three', 'd'=>'four');
    sort($tab);
    print_r($tab);
	//Array(
            [0] => four
            [1] => one
            [2] => three
            [3] => two
        )
</code></pre>