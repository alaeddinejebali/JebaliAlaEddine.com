<pre><code class="php">
	$tab1 = array('a'=>'one', 'b'=>'two', 'c'=>'three', 'd'=>'four');
	$tab2 = array('x'=>'five', 'b'=>'six', 'y'=>'seven', 'a'=>'one');
	$Res = array_intersect_key($tab1, $tab2);
	print_r( $Res );
	//outputs: Array ( [a] => one [b] => two )<br/>
	$Res = array_intersect_assoc($tab1, $tab2);
	print_r( $Res );
	//outputs: Array ( [a] => one )
</code></pre>