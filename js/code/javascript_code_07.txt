<pre><code class="javascript">
history.go(-2);
//return the user to the page before the previous page

history.go(3);
//go forward three pages

history.go(-1)
//is the same as:
back()

history.go(1)
//is the same as:
forward()
</code></pre>