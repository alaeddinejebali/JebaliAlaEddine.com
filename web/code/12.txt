<pre><code class="sh">
$> sudo apt-get install php7.0
$> sudo apt-get install libapache2-mod-php7.0
# the restart Apache
$> sudo service apache2 restart
# or using:
$> sudo /etc/init.d/apache2 restart
# then check Apache by visiting http://localhost
</code></pre>
